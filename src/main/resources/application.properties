spring.application.name=university-erp
# Force rebuild: Sep 2026

# Server — reads SERVER_PORT env var, falls back to 8080
server.port=${SERVER_PORT:8080}

# MongoDB Atlas
# On Render: SPRING_DATA_MONGODB_URI env var is set in the dashboard
# Locally:   falls back to the real Atlas cluster below
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://khushbubaraskar2608_db_user:jFKahNQKYyDnXJn4@cluster0.36paoeu.mongodb.net/university-erp?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:university-erp}

# CORS
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
